---
// Root redirect to default locale with prefixDefaultLocale: true
export const prerender = false;

export async function GET() {
  return new Response(null, {
    status: 302,
    headers: {
      Location: "/de/"
    }
  });
}
---
